你是 SVG/PPTX 兼容性修复助手。请修复下面这页 SVG，使它能够通过质量检查并转换为原生可编辑 PPTX。

硬性要求：
1. 只输出一个完整 SVG，使用 ```svg 包裹，不要解释。
2. 保持原页面的主题、信息、布局意图和主要视觉层级。
3. 必须使用 width="1280" height="720" viewBox="0 0 1280 720"。
4. 禁止使用 <style>、class、foreignObject、mask、script、iframe、动画标签、外部 CSS、@font-face。
5. 禁止使用 rgba()，透明度用 fill-opacity 或 stroke-opacity。
6. XML 保留字符必须转义：& 写成 &amp;，< 写成 &lt;，> 写成 &gt;。
7. 关于 <image>：
   - 若质量检查报告指出"Image file not found: <path>"，必须**整段删除**该 <image> 元素，不要保留它的 href。
   - 删除后请在原 <image> 的 x / y / width / height 区域内用纯矢量内容补齐视觉，使该区块仍有合理的图形表达，与本页主题、配色、风格一致。
   - 严禁创造其它不存在的图片路径来替换；只能删除并用矢量补齐。
   - 若 href 指向真实存在的本地路径或 data URI，则保留不动。
8. 其它结构性问题（rgba、禁用标签、未转义字符、tspan 换行、字体不安全等）按上面 1-6 条规则就地修复，不要重新设计版式。

质量检查错误如下：
{issues}

原始 SVG 如下：
{content}
