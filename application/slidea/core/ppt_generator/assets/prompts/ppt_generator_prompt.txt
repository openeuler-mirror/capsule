
严格遵守以下要求:
1. 合理控制内容量以避免垂直溢出，确保内容在 1280px × 720px 的尺寸内合理展示。
2. 使用一致的样式和色彩方案，与下方已有页面保持一致！
3. 任何地方都不要使用absolute布局，且各个内容块之间要有一定的间隔距离，尤其是上下块之间不要紧贴！！！
4. 不要添加右上角、右下角、左下角任何标志，也不需要添加页码。
5. 不要使用任何阴影（shadow）！！！不要使用任何阴影（shadow）！！！不要使用任何阴影（shadow）！！！
6. 不要使用 overflow: auto / scroll，以避免内容中出现滚动条。
7. 只有素材中有准确数据时才使用 chart.js生成图表（折线图、柱状图、饼图等），不使用动画效果，不要杜撰数据。
   Canvas 容器规则（与规则 7 同思路）：
   - 容器显式声明 style="flex: N 1 0; min-height: 0;" 按比例分配空间
   - 内层用 flex items-center justify-center 包裹 canvas
   - Chart.js 配置必须设置 responsive: true 和 maintainAspectRatio: false，
     让图表填满容器，而不是反向撑大容器
   - 不要给 canvas 写固定像素高度
   ✅ 正确示范：
   <div class="flex flex-col" style="flex: 2 1 0; min-height: 0;">
       <p class="flex-none mb-2">图表标题</p >
       <div class="flex-1 flex items-center justify-center" style="min-height: 0;">
           <canvas id="myChart"></canvas>
       </div>
   </div>
   <script>
       new Chart(document.getElementById('myChart'), {
           type: 'bar',
           data: {...},
           options: { responsive: true, maintainAspectRatio: false, animation: false }
       });
   </script>
8. 图片应使用 max-w-full 和具体的 max-h-[x] 来限制大小，保持 w-auto h-auto 以确保真实占位，并配合父级的 Flex 居中属性！！！
    例如：<img src="images/c627d5816a391c42ff05085ee0d4e571.jpg" alt="戈壁科研" class="max-w-full max-h-48 w-auto h-auto">。
    不要使用参考资料中没有的图片！不要使用参考资料中没有的图片！不要使用参考资料中没有的图片！
9. 图片占位与尺寸约束：
    a) 只能使用参考资料中提供的图片，不要使用参考资料中没有的图片。
    b) 图片必须水平垂直居中。
    c) 严禁使用 max-h-[具体像素] 这类需要 AI 估算的高度写法。图片高度一律由 flex 容器分配，图片自身只负责"不超出分配的空间"。
    d) 图片所在的 flex 子项必须按比例分配空间（不要让 AI 估算具体像素高度）：
       - 容器显式声明 style="flex: N 1 0; min-height: 0;"（N 是相对比例，如 3、2、1）
       - 内层用 flex items-center justify-center 居中
       - 图片用 class="max-w-full w-auto h-auto" + style="max-height: 100%; object-fit: contain;"
       
       原理：flex 子项默认 min-height: auto 会被图片固有尺寸撑破；
       图片用 max-height: 100% 始终被父容器约束，object-fit: contain 保证不变形不裁切；
       不要依赖 justify-around / space-y 自然撑开图片位，必须用 flex 比例显式分配。
    ✅ 正确示范：
    <div class="flex flex-col" style="flex: 3 1 0; min-height: 0;">
        <p class="flex-none mb-2">标题</p >
        <div class="flex-1 flex items-center justify-center" style="min-height: 0;">
            < img src="images/xxx.png" alt="..." 
                 class="max-w-full w-auto h-auto"
                 style="max-height: 100%; object-fit: contain;">
        </div>
    </div>
    ❌ 错误示范：
    <!-- 错1：用 max-h-[具体像素] 估算高度 -->
    < img src="images/xxx.png" class="max-w-full max-h-[320px] w-auto h-auto">
    
    <!-- 错2：缺 min-height: 0，图片会顶破容器 -->
    <div class="flex-1 flex flex-col justify-around">
        <div class="flex items-center justify-center">
            < img src="images/xxx.png">
        </div>
    </div>
10. 多组件垂直排列的高度锁定与平分规则（解决垂直溢出核心问题）：
    a) 当多个内容卡片（Card）、列表项或文本块在垂直方向排列时，严禁任由其随文字内容自然撑开高度，这会导致内容顶破 720px 的底边。
    b) 必须通过 Flex 比例显式分配垂直空间。父容器设置 flex flex-col，子项（如每一条卡片）必须声明 style="flex: 1 1 0; min-height: 0;"。
    flex: 1 1 0 确保所有卡片在容器内绝对平分剩余高度，不因文字多寡而变形。
    min-height: 0 是关键：它允许容器的大小小于其内部文字的自然高度，配合整体缩放脚本工作。
    c) 内容块内部必须配合 justify-center 或 justify-between 以及 min-h-0
    d) 内容块之间必须使用 gap-N 或 space-y-N 保持间距，且间距不宜过大（建议 gap-2）。
    e) min-height: 0 必须从父链顶端一路传到图片所在 flex 子项，中间任何一层 flex 容器（含 .section-container、.scalable-content、内层 wrapper、行/列容器）只要少一层，图片固有高度就会反向撑破容器，导致"加图前占位小、加图后变大"。所有嵌套的 flex 父容器都要显式声明 style="min-height: 0;"（Tailwind 的 min-h-0 也可，但注意 min-height-0 是无效写法）。

11.禁止采用“全宽纵向堆叠”的布局方式！！！禁止采用“全宽纵向堆叠”的布局方式！！！禁止采用“全宽纵向堆叠”的布局方式！！！应采用多列或网格化布局，利用横向空间以避免垂直高度过大导致缩放失效。
12. 严禁使用任何 emoji 字符作为图标、标签前缀或装饰元素（例如 📚、📅、✍️、🚀 等），因为导出链路中 emoji 容易丢失或显示为方框。
13. `<head>`中的外部资源引用必须严格遵守以下规则，不允许自行改写域名，不允许输出看起来相似但错误的地址。
脚本资源必须使用且只能使用以下固定地址：
```html
<script src="https://cdn.jsdmirror.com/npm/tailwindcss-cdn@3.4.10/tailwindcss.js"></script>
<script src="https://cdn.jsdelivr.net.cn/npm/chart.js"></script>
```
字体 `<link>` 必须从 `https://fonts.googleapis.cn/css2?...` 获取，并且只选择当前页面主语言实际需要的 Google 字体，不要使用 `fonts.googleapis.com`、`fonts.googleapis.com.cn` 或其他域名变体。
模板中的默认字体链接是中文 `Noto Sans SC` 基线；如果页面主语言不是中文，请保持脚本资源地址不变，只替换字体 `<link>` 和 `body, html` 中的 `font-family`，并参考下面的日文、韩文示例。
严禁输出 `https://fonts.googleapis.com/...`、`https://fonts.googleapis.com.cn/...`、`https://cdn.tailwindcss.com`、`https://cdn.jsdelivr.net/...` 等其他变体。
字体必须与模板保持完全一致，优先直接复用模板中的字体声明。不要输出复杂的 `html:lang(...)` 字体切换规则，而是直接在 `body, html` 中按页面主语言写死字体。该规则应通用于任意语言；下面只给出中日韩三个示例：
简体中文页面示例：
<link href="https://fonts.googleapis.cn/css2?family=Noto+Sans+SC:wght@400;700&amp;display=swap" rel="stylesheet"/>
body, html {
    font-family: 'Noto Sans SC', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #ffffff;
}
日文页面示例：
<link href="https://fonts.googleapis.cn/css2?family=Noto+Sans+JP:wght@400;700&amp;display=swap" rel="stylesheet"/>
body, html {
    font-family: 'Noto Sans JP', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #ffffff;
}
韩文页面示例：
<link href="https://fonts.googleapis.cn/css2?family=Noto+Sans+KR:wght@400;700&amp;display=swap" rel="stylesheet"/>
body, html {
    font-family: 'Noto Sans KR', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #ffffff;
}
其他语言页面也遵循同样模式：在 `body, html` 中直接写当前页面主语言对应的字体族，并确保该字体通过 `https://fonts.googleapis.cn/css2?...` 正确引入。

14. 禁止使用任何半透明效果、模糊滤镜与渐变，包括但不限于 `rgba(...)`、`hsla(...)`、`transparent`、`opacity-*`、`opacity:`、`backdrop-filter`、`-webkit-backdrop-filter`、`filter: blur(...)`。PDF 导出对透明度和模糊的兼容性很差，容易导致偏色、发粉、卡片或文字整块丢失。若需要柔和层次，请只使用纯色背景、纯色边框。
15. `<head>`中必须引用一下资源和规则，不允许自行改写域名，不允许输出看起来相似但错误的地址，以支持渲染行级公式和块级公式。
```html
    <link rel="stylesheet" href="https://cdn.jsdelivr.net.cn/npm/katex@0.16.9/dist/katex.min.css">
    <script defer src="https://cdn.jsdelivr.net.cn/npm/katex@0.16.9/dist/katex.min.js"></script>
    <script defer src="https://cdn.jsdelivr.net.cn/npm/katex@0.16.9/dist/contrib/auto-render.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            renderMathInElement(document.body, {
                // 自定义定界符
                delimiters: [
                    {left: '$$', right: '$$', display: true},  // 块级公式
                    {left: '$', right: '$', display: false}, // 行内公式
                    {left: '\\(', right: '\\)', display: false},
                    {left: '\\[', right: '\\]', display: true}
                ],
                throwOnError : false // 遇到错误公式不报错，而是输出红字
            });
        });
    </script>
```

重要事项!!!:
创建的PPT需要使用以下示例中完全一致的缩放机制,即:
1. 一定要有
#ppt-container

.section-container

.scalable-content
这三个css.
2. 必须严格遵守以下 Flexbox 布局规范，以配合自动缩放脚本实现完美垂直分布：
#### 1. 顶级容器架构 (Parent Container)
每一个独立的板块（Section）必须遵循“标题+内容”的结构，并使用以下包裹层：
*   **类名要求**：`<div class="flex-1 flex flex-col">`
*   **禁止事项**：**绝对禁止**在该层级使用 `justify-center`、`justify-between` 或 `justify-around`。该层级仅用于自然堆叠标题和内容区。
#### 2. 内容区布局 (Content Body)
标题下方的“内容部分”是布局的核心，**必须**包裹在以下容器中以实现自动分布：
*   **类名要求**：`<div class="flex-1 flex flex-col justify-around">`
*   **针对多行布局的补充**：当内容区包含多行卡片或多个主要块时，该容器应作为“垂直分配器”。
    *   *注：如果是横向排列，请将 `flex-col` 改为 `flex-row`，但 `flex-1` 和 `justify-around` 是必须保留的核心属性。*
*   **作用机制**：利用 `flex-1` 占据所有剩余空间，利用 `justify-around` 自动计算并分配子元素之间的空隙。
#### 3. 间距控制原则 (Spacing Strategy)
*   **允许使用最小保护间距**：在存在多行内容（如第一排和第二排卡片）时，**允许且建议**在内容区容器（即带有 `justify-around` 的那一层）使用 `space-y-*`（建议 `space-y-6` 或 `space-y-8`）。
    *   *原理：* `space-y` 提供了一个**保底的最小间距**。当页面空间充裕时，`justify-around` 会在此基础上继续拉开距离；当页面空间紧张时，`space-y` 确保两行不会紧贴。
*   **禁用外边距**：**严禁**给主要子元素（卡片本身）添加 `mb-*` 或 `mt-*`。
*   **正确做法**：
    1.  使用父容器的 `justify-around` 处理大块之间的动态拉伸。
    2.  使用父容器的 `space-y-*` 处理多行之间的物理隔离保底。
    3.  卡片内部的微小组件（图标与文字之间）可自由使用 `space-x-*` 或 `gap-*`。

在 Flex 纵向布局中使用justify-around时，必须确保容器显式设置高度（如 h-full、h-[50%]等）以占据剩余空间，避免高度坍塌导致对齐失效！！！

#### ✅ 代码范例对比
**❌ 错误示范 (使用 space-y 或 margin 控制间距)：**
```html
<!-- 错误：使用了 space-y-6 来强行推开间距 -->
<div class="space-y-6">
    <div class="flex items-start">
        <div class="circle-number">1</div>
        <div>...内容...</div>
    </div>
    <div class="flex items-start">
        <div class="circle-number">2</div>
        <div>...内容...</div>
    </div>
</div>
```
**✅ 正确示范 (使用 flex-1 和 justify-around 自动分布)：**
```html
<!-- 正确：父容器撑满剩余空间，子元素自动均匀分布 -->
<div class="flex-1 flex flex-col justify-around">
    <div class="flex items-start">
        <div class="circle-number">1</div>
        <div>...内容...</div>
    </div>
    <div class="flex items-start">
        <div class="circle-number">2</div>
        <div>...内容...</div>
    </div>
</div>
```
3. 一定要有
<script>
    function scaleContentToFit() {
        const sections = document.querySelectorAll('.section-container');
        sections.forEach(section => {
            const contentWrapper = section.querySelector('.scalable-content');
            const content = contentWrapper ? contentWrapper.firstElementChild : null;
            if (!contentWrapper || !content) return;

            contentWrapper.style.transform = 'scale(1)';
            contentWrapper.style.zoom = 1;

            const styles = window.getComputedStyle(section);

            const paddingTop = parseFloat(styles.paddingTop);
            const paddingBottom = parseFloat(styles.paddingBottom);
            const paddingLeft = parseFloat(styles.paddingLeft);
            const paddingRight = parseFloat(styles.paddingRight);

            const ORIGINAL_HEIGHT = section.clientHeight - paddingTop - paddingBottom;
            const ORIGINAL_WIDTH = section.clientWidth - paddingLeft - paddingRight;
            if (Math.abs(contentWrapper.scrollWidth - ORIGINAL_WIDTH) < 1 && Math.abs(contentWrapper.scrollHeight - ORIGINAL_HEIGHT) < 1) {
                window.final_ratio = 1;
                return;
            }

            const TARGET_ASPECT_RATIO = ORIGINAL_HEIGHT / ORIGINAL_WIDTH;

            let testWidth = ORIGINAL_WIDTH;

            let bestWidth = ORIGINAL_WIDTH;
            let minDifference = Infinity;

            for (let i = 0; i < 300; i++) {
                testWidth = testWidth + 5;
                content.style.width = `${testWidth}px`;

                const actualHeight = content.scrollHeight;
                const targetHeight = testWidth * TARGET_ASPECT_RATIO;

                const currentDifference = Math.abs(actualHeight - targetHeight);

                if (currentDifference < minDifference) {
                    minDifference = currentDifference;
                    bestWidth = testWidth;
                }
            }

            content.style.width = `${bestWidth}px`;

            const finalWidth = content.scrollWidth;
            const finalHeight = content.scrollHeight;

            const scaleX = ORIGINAL_WIDTH / finalWidth;
            const scaleY = ORIGINAL_HEIGHT / finalHeight;

            const scaleRatio = Math.min(scaleX, scaleY);
            window.final_ratio = scaleRatio;
            contentWrapper.style.transform = 'none';
            contentWrapper.style.zoom = scaleRatio;
        });
    }

    window.addEventListener('load', scaleContentToFit);
    window.addEventListener('resize', scaleContentToFit);
</script>
这个缩放脚本。并且不要做任何修改！不要做任何修改！不要做任何修改！
只有通过这样一步步的缩放，才能找到最小的最合适的"自然缩放比例"。
不是简单地想把内容塞进容器，而是希望通过微调内容的原始（未缩放时）尺寸，找到一个“最佳渲染点”，使得内容在换行、布局上达到一个最理想的状态。
脚本的介绍如下：
**目标：** 为了确保您生成的HTML/CSS布局能够与项目中使用的特殊缩放脚本 完美兼容，请仔细阅读并遵守以下指南。

#### **1. 脚本简介：它是一个“试探性”布局修正脚本**

这个脚本的核心功能不是简单的等比缩放。它的工作方式更像一个“智能整理师”：

*   **第一步：探索性放大 (Exploratory Expansion)**
    它会通过一个循环，逐渐增加内容区域的总宽度。在每一步放大中，它会实时测量内容的总高度。

*   **第二步：寻找最佳平衡点 (Finding the Optimal Balance)**
    它的目标是找到一个宽度和高度的“理想”组合，使得内容能够最舒展、最自然地排布。它认为，当内容的测量高度与基于宽高比计算出的理论高度非常接近时，就找到了这个平衡点。

*   **第三步：精确缩放至容器 (Precise Scaling)**
    一旦找到这个“理想尺寸”，它就会计算出一个精确的缩放比例（`zoom`），将这个已经“整理好”的内容完美地缩放到父容器中。

#### **2. 关键协作原则：保持高度的“可预测性”**

这个脚本成功的关键，在于它能够**预测**增加宽度后，高度会如何变化。如果内容的CSS导致高度以一种**不可预测**或**非线性**的方式变化，脚本的计算就会失败。

因此，您在生成HTML和CSS时，必须遵循以下核心规则：

**【规则1：绝对禁止使用“宽高比锁定”的CSS技巧】**

**严禁**在任何元素上使用 `padding-top` 或 `padding-bottom` 的百分比值来维持宽高比。

*   **错误示例 (会导致脚本崩溃):**
    ```css
    .image-container {
        /* 这个技巧会让高度随宽度变化，脚本无法处理 */
        padding-top: 56.25%; /* 16:9 ratio */
        height: 0;
        position: relative;
    }
    ```

*   **为什么会冲突?**
    这个技巧会创建一个“宽度->高度”的直接依赖关系。当脚本增加宽度时，您设置的 `padding-top` 会导致元素高度也随之增加。这形成了一个脚本无法解决的循环依赖，导致最终计算出错误的、极小的缩放比例。

*   **正确替代方案:**
    请使用 `flexbox` 或 `grid` 布局来管理尺寸。对于图片，可以直接设置其 `width: 100%` 和 `height: 100%`，或者让其在flex容器中自适应。

**【规则2：优先使用Flexbox进行空间分配】**

请大量使用 `display: flex` 和 `flex: 1` 属性来让元素自动填充可用空间。

*   **推荐示例 (与脚本完美兼容):**
    ```css
    .container {
        display: flex;
        flex-direction: column;
        height: 100%;
    }
    .card {
        flex: 1; /* 自动均分父容器的高度 */
        min-height: 0; /* 防止内容溢出 */
    }
    ```
*   **为什么这样好?**
    Flexbox布局的高度分配是稳定且可预测的。当脚本改变总宽度时，flex项的高度分配逻辑保持不变，不会产生意外的高度增长。这为脚本的计算提供了稳定的环境。

**【规则3：避免使用固定的`height`值（除非必要）】**

尽量避免给主内容区的模块设置固定的像素高度（如 `height: 300px`）。让它们的高度由Flexbox或其内部内容自然决定。这给了脚本在“试探”过程中更大的灵活性。

#### **3. 总结**

该脚本是一个强大的布局优化工具，但它依赖于一个**稳定**且**可预测**的CSS环境。您的任务是提供这样的环境。

**请记住，您的首要任务是：确保任何元素的尺寸变化（尤其是高度）不会与脚本的宽度试探过程产生意外的耦合。**

遵守这些指南，您生成的代码将能充分利用这个脚本的优势，创造出既美观又健壮的布局。
4. 一定要在主要内容外使用
<div class="section-container">
<div class="scalable-content">
</div>
</div>
包裹住主要内容!

